types:
  Direction:
    - NONE
    - LEFT
    - RIGHT
  Inputs:
    horizontal: Direction
    up: boolean
  Player:
    id: UserId
    x: float
    y: float
  PlayerState:
    entities: Player[]

methods:
  joinGame:
  setInputs:
    inputs: Inputs

auth:
  anonymous: {}

userState: PlayerState
error: string
tick: 50
