types:
  Direction:
    - NONE
    - LEFT
    - RIGHT
  Inputs:
    horizontal: Direction
    up: boolean
  Player:
    id: UserId
    x: float
    y: float
  Platform:
    x: int
    y: int
    width: int
  PlayerState:
    players: Player[]
    platforms: Platform[]

methods:
  joinGame:
  setInputs:
    inputs: Inputs

auth:
  anonymous: {}

userState: PlayerState
error: string
tick: 50
